# Next Steps

1. Complete TASK-0004 release-readiness polish.
2. Keep `RepositoryUrl` as TODO until the real remote URL is intentionally selected.
3. Run `ackit generate --target all` only when generated files should be committed.
4. Do not push, publish, redact, or delete without explicit maintainer action.

# Next Steps

1. Complete TASK-0002 pseudonym replacement.
2. Add `--json` output for script/CI use.
3. Validate local pack and temporary tool-path install.
4. Keep `RepositoryUrl` as TODO until the real remote URL is intentionally selected.
5. Decide later whether to keep `AgentContextKit.slnx` alongside the required `AgentContextKit.sln`.
6. Run `ackit generate --target all` only when generated files should be committed.
7. Do not push, publish, redact, or delete without explicit maintainer action.

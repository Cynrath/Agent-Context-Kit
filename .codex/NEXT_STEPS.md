# Next Steps

1. Complete TASK-0005 release candidate review.
2. Keep `RepositoryUrl` and `PackageProjectUrl` as TODO until the real remote URL is intentionally selected.
3. Run `ackit generate --target all` only when generated files should be committed.
4. Do not push, publish, redact, tag, or delete without explicit maintainer action.
